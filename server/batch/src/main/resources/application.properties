# ?? DB ?? properties

# main ???? 8080, match 8081, batch 8082
server.port=8082


spring.datasource.url=jdbc:postgresql://192.168.0.159:5432/postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=qjarms98#

# Batch should not recreate schema on startup when it connects to the shared DB.
# Use update for now; consider validate/none once the schema management policy is fixed.
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.highlight_sql=true


logging.level.org.hibernate.orm.jdbc.bind=TRACE

# MyBatis XML ?? ?? ??
mybatis.mapper-locations=classpath:mapper/**/*.xml
# MyBatis snake -> camel ??
mybatis.configuration.map-underscore-to-camel-case=true

# spring.data.redis.host=localhost
# spring.data.redis.port=6379

# Spring Batch metadata tables are required for Job execution history.
# Enable schema initialization for the batch module's database.
spring.batch.jdbc.initialize-schema=always
